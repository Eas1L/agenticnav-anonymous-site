# 匿名网站维护

## 网站入口与文件

- 网页入口：`docs/index.html`。
- 页面样式：`docs/static/css/index.css`。
- 页面交互：`docs/static/js/index.js`。
- 插图：`docs/static/images/`；视频完成后放到 `docs/static/videos/`。
- 网页中只使用相对路径加载素材，不引用署名网站、署名预印本或个人账号。
- 不把工作日志、原始实验文件、个人路径、授权凭据或旧仓库历史放入本仓库。

## Anonymous GitHub 发布设置

1. 源分支选择 `main`，GitHub Pages 来源设置为该分支的 `/docs`。
2. 勾选 **Auto-update from GitHub**，跟踪分支的新提交，不锁定某一次提交。
3. 启用图片与 **GitHub Pages** 展示；需要视频时检查媒体支持及大小限制。
4. 不使用到期跳转至原始 GitHub 仓库的选项；到期时间需覆盖审稿期，届时可通过管理页延长。
5. 匿名标识一旦写入论文就保持不变；更新时修改现有记录，不删除重建。
6. 使用明确指向 `index.html` 的网页地址；服务的目录地址在非浏览器访问中可能返回含源仓库线索的内容。匿名入口不能消除公开源仓库本身带来的关联。

## 后续更新

1. 修改页面或加入已匿名检查的视频；确认页面和所有素材都没有姓名、单位、标志、人脸、署名论文链接、个人账号和文件元数据。
2. 提交并推送到 `main`。
3. Anonymous GitHub 按页面说明至多每小时检查一次更新，不保证提交后立即显示。需要立即更新时，在该匿名项目管理页使用刷新功能。
4. 用原来的匿名网址检查新文字或新视频已经显示；网址本身不需要更换。
5. 技术上可更新与会议是否允许在特定时间更新是两件事，实际内容变更应遵守投稿和审稿要求。

## 当前发布状态

- 初版素材：五张工具与方法插图，未加入视频或论文下载。
- 已创建匿名项目并启用自动更新、图片与网页展示；到期时间为2027年3月16日，到期不跳转至原始仓库。
- 匿名网址保存在作者的独立工作区记录中，不在公开源仓库中增加指向匿名入口的反向链接。
- 源仓库为公开仓库，Pages 来自 main 的 /docs；服务通过公开仓库读取，不需要安装应用以读取私有仓库。
- 保留原始提交历史是本仓库自身的更新记录，不包含任何先前署名仓库的历史。
